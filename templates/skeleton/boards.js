module.exports = {
  // Where your application code is located
  appRoot: `${__dirname}/src`,

  // Where you keep your project's own templates
  templateRoot: null,

  // Default parameters used throughout tasks (includes presets)
  parameters: {},

  // Your tasks.
  tasks: {}
};
